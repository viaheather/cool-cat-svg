const generateSVG = 

// if SVG square
`
<svg width="500" height="500">
  <rect x="50" y="20" width="500" height="500" style="fill:blue" />
  Sorry, your browser does not support inline SVG.  
</svg>
`

// if SVG circle
`
<svg width="500" height="500">
<circle cx="200" cy="200" r="200" fill="red" />
</svg>
`

// if SVG Triangle
`
<svg height="500" width="500">
  <polygon points="200,10 400,300 10,300" style="fill:black" />
  Sorry, your browser does not support inline SVG.
</svg>
`

generateSVG(answers) {
    
}