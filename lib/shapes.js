class Shape {
    constructor (x, y, height, fill) {
        this.x = x;
        this.y = y;
        this.height = height;
        this.fill = fill;
    }
}
class Circle {
    constructor(cx,cy,r) {
    super(cx,cy,r,fill);
    }
}

class Triangle extends Shape {
    constructor(x,y,height,fill) {
        super(x,y,height,fill);
        this.points = points;
    }
} 

class Square extends Shape {
    constructor (x,y,height,fill) {
        super(x,y,height,fill);
        this. 
    }
}